# Copy to .env (gitignored). Compose reads this for the app service; host-native
# runs fall back to the same defaults baked into dev_project/settings/base.py.
POSTGRES_DB=django_assets_test
POSTGRES_USER=django_assets
POSTGRES_PASSWORD=django_assets
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
# DJANGO_SECRET_KEY=generate-one-for-anything-beyond-local-dev
