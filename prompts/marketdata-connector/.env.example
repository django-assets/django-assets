# Copy to `.env` (gitignored) and fill in the real value.
# MarketData.app Bearer token — emailed on signup, or from https://www.marketdata.app/dashboard/
# The connector and the MarketData Python SDK both read this from the environment.
MARKETDATA_TOKEN=your_api_token_here
